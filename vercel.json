{
  "version": 3,
  "builds": [
    {
      "src": "api/**/*.ts",
      "use": "@vercel/node",
      "config": { "includeFiles": ["**/*.ts"] }
    }
  ],
  "routes": [
    { "src": "/api/(.*)", "dest": "api/$1.ts" }
  ]
}
